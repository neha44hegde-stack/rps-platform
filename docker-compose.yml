services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=change-this-in-production
      - FLASK_CONFIG=production
    volumes:
      - ./instance:/app/instance
